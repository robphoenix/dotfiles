# PR Prep

Stage changes, run `pnpm checks`, create a changeset, commit with a
conventional message. Then STOP and print the exact `git push` and `gh pr
create` commands for me to run manually (sandbox cannot push).
